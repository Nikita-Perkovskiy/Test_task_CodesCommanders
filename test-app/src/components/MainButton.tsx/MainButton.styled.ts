import { darkGray } from "../../constants.ts";

export const styles = {
  mainButton: {
    all: "unset",
    boxSizing: "border-box",
    width: "90px",
    height: "40px",
    textAlign: "center",
    backgroundColor: `${darkGray}`,
    borderRadius: "20px",
    cursor: "pointer",
  },
};
